[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"



[deploy.env]
# ⚠️ IMPORTANTE: Configurar OPENAI_API_KEY en Railway Dashboard
# No incluir API keys en este archivo
PORT = "8080"
PYTHONUNBUFFERED = "1"
FLASK_ENV = "production"
